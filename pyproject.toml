[project]
name = "microduck-genesis"
version = "0.1.0"
description = "Genesis World setup and MicroDuck import validation"
readme = "README.md"
requires-python = ">=3.12,<3.14"
dependencies = [
  "genesis-world==1.4.1",
  "torch>=2.8",
  "psutil",
  # Import name is `bam`; this is the exact upstream MicroDuck actuator source.
  "better-actuator-models @ git+https://github.com/Rhoban/bam.git@62bd8ce12154340be97e06f7f41a0ca8f116d967",
]

[tool.uv]
package = false
