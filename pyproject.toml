[project]
name = "microduck-genesis"
version = "0.1.0"
description = "Genesis World setup and MicroDuck import validation"
readme = "README.md"
requires-python = ">=3.12,<3.14"
dependencies = [
  "genesis-world==1.4.1",
  "torch>=2.8",
  "psutil",
]

[tool.uv]
package = false
